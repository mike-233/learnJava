package sn.mike.programme;

// on ne peut mettre que des methodes abstraite
//public abstract boolean ispeeled();we can remove abstract because the compilateur can put it
// automatique. we can build many implements

public interface PeelFruit{
	public boolean ispeeled();
	public String getSkinType();
}
